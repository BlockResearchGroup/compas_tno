****************
The masonry arch
****************

.. _example-arch:

This example finds the minimum and maximum thrust for a circular arch based on it's span (L), height (H) and thickness (thk). The points where the thrust line touch intrados (resp. extrados) are highlighted in blue (resp. green).

.. figure:: ../_images/arch.png
    :figclass: figure
    :class: figure-img img-fluid


.. literalinclude:: ../../examples/_tutorial/1_arch.py
